### Setup Agent

Add folder x509 in internal/data/

```
mkdir internal/data/x509
```

Now add ca_cert.pem to the x509 directory

Finally make proto:

```
make proto
```

### Run Agent

```
make run
```